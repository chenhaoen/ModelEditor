set(RENDERINGCONTEXTDRIVER_SRC_DIR ${MODELEDITOR_SRC_DIR}/${PROJECT_NAME}/RenderingContextDriver)
set(RENDERINGCONTEXTDRIVER_INCLUDE_DIR ${MODELEDITOR_INCLUDE_DIR}/${PROJECT_NAME}/RenderingContextDriver)

file(GLOB RENDERINGCONTEXTDRIVER_INCLUDE_FILES "${RENDERINGCONTEXTDRIVER_INCLUDE_DIR}/*.h")

file(GLOB RENDERINGCONTEXTDRIVER_SRC_FILES "${RENDERINGCONTEXTDRIVER_SRC_DIR}/*.cpp")

target_sources(Core 
  PRIVATE 
    ${RENDERINGCONTEXTDRIVER_INCLUDE_FILES}
    ${RENDERINGCONTEXTDRIVER_SRC_FILES}
)